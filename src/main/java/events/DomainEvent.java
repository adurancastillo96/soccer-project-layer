package events;

/**
 * Marker interface for all main.java.domain main.java.events emitted by the system. A main.java.domain event
 * represents a significant occurrence in the main.java.domain main.java.model that may be of
 * interest to multiple parts of the application (UI, main.java.persistence, logging,...).
 */
public interface DomainEvent {
    // no methods required
}
